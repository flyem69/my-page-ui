import { AfterElementInitDirective } from './after-element-init.directive';

describe('AfterElementInitDirective', () => {
	it('should create an instance', () => {
		const directive = new AfterElementInitDirective();
		expect(directive).toBeTruthy();
	});
});
